<h1>Carrito</h1>
<div class="container_cart">
    {{#each products}}
        <div class="container_product_cart">
            <p>Producto: {{this.product.title}}</p>
            <p>${{this.product.price}}</p>
            <p>Cantidad:{{this.quantity}}</p>
        </div>
    {{/each}}
</div>