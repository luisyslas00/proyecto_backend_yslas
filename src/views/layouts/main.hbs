<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/{{styles}}">
    {{!-- Google Font - Roboto --}}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.12.1/css/all.css" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">
    <title>{{title}}</title>
</head>
<body>
    <header class="header">
        <a href="/" class="header_logo slide_down_logo">Home</a>
        {{#if user}}
            <div class="div_user">
                <span><i class="fa fa-user"></i> {{user}}</span>
                <a href="/api/sessions/logout">Salir</a>
            </div>
        {{else}}
            <div class="div_user">
                <span><i class="fa fa-user"></i></span>
                <a href="/login">Ingresar</a>
            </div>
        {{/if}}
        <ul class="header_menu">
            <li><a href="/products" class="slide_down">Products</a></li>
            <li><a href="/chat" class="slide_down"href="">Contact</a></li>
        </ul>
        {{#if cartID}}
        <a href="/carts/{{cartID}}" id="btn_cart" data-id="{{cartID}}"><i class="fa fa-shopping-cart"></i></a>
        {{else}}
        <a href="/login" id="btn_cart"><i class="fa fa-shopping-cart"></i></a>
        {{/if}}
    </header>
    {{{body}}}
</body>
</html>